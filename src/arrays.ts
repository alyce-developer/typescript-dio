// arrays (ou vetor):
let dados: string[] = ["alyce", "maria"];
// let maisDados: Array<string> = ["maria", "alyce"];
let dadosNumericos: number[] = [1, 2, 3];

// arrays de multi tipos, armazena dois tipos de tipos diferentes
let infos: (string | number)[] = ["alyce", 24];
let maisInfos: (number | boolean)[] = [24, true];

// --------!!----------

// alguns exemplos de métodos de arrays:

// mapeia um objeto
// dados.map()

// remove o ultimo elemento do array
// dados.pop()

// filtra uma coisa especifica
// dados.filter()
